[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-music-generalization"
version = "0.1.0"
description = "Unified benchmark scaffold for generalized AI-music detection"
requires-python = ">=3.10"
